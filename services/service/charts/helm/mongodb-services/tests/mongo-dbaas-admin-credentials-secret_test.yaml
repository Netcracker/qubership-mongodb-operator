suite: Test Mongo Dbaas Admin credentials secret
templates:
  - secrets/mongo-dbaas-admin-credentials-secret.yaml
tests:
  - it: should render secret from default values
    asserts:
      - equal:
          path: stringData.username
          value: "dbaas"
      - equal:
          path: stringData.password
          value: "dbaas"