waitSeconds: "400"
schemaSettings:
  schemaType: single
mongodb:
  install: true
  storage:
    size:
      - 2Gi
    storageClasses:
      - local-path
backup:
  install: true
  storage:
    waitPvcBound: false
    size:
      - 1Gi
    storageClasses:
      - local-path
dbaas:
  install: true
prometheusExporter:
  install: true
robotTests:
  install: true
vaultRegistration:
  enabled: true
  url: http://vault-service-vault-service.ci-master.openshift.sdntest.netcracker.com
  token: hvs.DQMRk7f2yElKxITjn9LCGo3U
tls:
  mode: requireTLS
ESCAPE_SEQUENCE: "true"
